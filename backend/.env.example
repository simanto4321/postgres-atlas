# Postgres Atlas configuration

# Live mode — introspect a real database:
# ATLAS_DSN=postgres://user:pass@host:5432/dbname?sslmode=require

# Demo mode — serve a precomputed report (default for docker-compose):
ATLAS_SNAPSHOT=../docs/sample-report.json

ATLAS_ADDR=:8000
ATLAS_THRESHOLD_GB=100
